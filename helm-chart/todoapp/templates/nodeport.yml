apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-nodeport
  namespace: {{ .Values.todoapp.ns.name }}
spec:
  type: NodePort
  selector:
    app: {{ .Values.todoapp.ns.name }}
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    nodePort: 30007
